fn main() {
    let mut v = vec![1, 2, 3, 4, 5];

    // v.push(6);

    let first = &v[0];

    v.push(6);

    println!("the first element is {}", first);

    match v.get(5) {
        Some(sixth) => println!("The sixth element is {}", sixth),
        None => println!("There is no sicth element"),
    }
}
